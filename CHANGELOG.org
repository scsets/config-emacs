# $Id$
#+title: Emacs Configuration Changelog
#+author: scs

* Document role

Historical, user-visible changes.  Not the open backlog (=tasks.org=).
Formerly =changes.org=; renamed to =CHANGELOG.org= on 2026-07-10.

* 2026-07-24 Fri Local gitea.el against workstation Gitea

- init.el: =use-package= =gitea= from =~/src/gitea.el= with host
  =http://127.0.0.1:3000= and user =scs=; token from
  =~/.config/gitea/access-token= (not inlined).
- Deps: =markdown-mode=, =magit-section=, =with-editor=, =transient=
  (no =fedi.el= / =tp.el=; HTTP and compose are in-tree in gitea.el).

* 2026-07-24 Fri Nice Helm catalog UI on trunk

- lisp/scs-command-hub-helm.el: section headers, aligned title/keys/
  summary columns, and colored faces (extracted from the Hydra trial
  so that branch can be dropped cleanly).
- lisp/scs-command-hub.el: require the Helm UI module; Transient portal
  unchanged.
- init.el: autoload browse from the Helm module; reclaim ~C-c /~ and
  ~C-c ?~ on ~org-mode-map~ (Org had shadowed the portal).
- test/scs-command-hub-test.el: group + format checks.

* 2026-07-24 Fri Transient q quits hub from any panel

- lisp/scs-command-hub.el: every hub Transient shows ~q~ ->
  ~transient-quit-all~.
- init.el: ~transient-base-map~ also binds ~q~ to ~transient-quit-all~
  so it works anytime in a Transient menu (~C-g~ still pops one level).

* 2026-07-24 Fri Transient hub display: taller + brighter

- init.el (~use-package transient~): punchier ~transient-heading~ and
  semantic key faces (stay/exit/recurse/stack/return) for light/dark.
- Transient always fits its side window to the menu text, so a bare
  ~window-height~ alist entry stays minibuffer-sized for short hubs.
  ~scs/transient--enforce-min-height~ advises
  ~transient--fit-window-to-buffer~ and grows to ~28%~ of the frame
  (at least 10 lines).  Tune ~scs/transient-min-height-fraction~.

* 2026-07-24 Fri Command hub also on C-c /

- init.el: bind ~C-c /~ to ~scs/command-hub~ (same as ~C-c ?~, no
  Shift).  ~M-s o~ remains ~helm-occur~; ~plan.org~ once floated
  ~C-c /~ for occur -- hub wins.

* 2026-07-24 Fri Command hub (Transient + Helm catalog)

- lisp/scs-command-hub.el: curated plist catalog, Helm browse/describe,
  Transient home on ~C-c ?~, Org/TRAMP/Frames workflow panels.
- test/scs-command-hub-test.el: ERT for catalog accessors/validation.
- init.el: Helm ~C-h~ help/debug family cleared (~?~ = helm-help);
  ~helpful~ + explicit ~transient~ (and deps); autoload + ~C-c ?~.
- readme.org / insights.org: operator and design notes.

* 2026-07-24 Fri scs/org- interactive prefix for export commands

- lisp/org-tools.el: rename interactive export entry points to
  ~scs/org-line-prefixes-mode~, ~scs/org-line-prefixes-status~,
  ~scs/org-regenerate-stationery~; library ~org-tools-*~ unchanged.
  ~scs/rename-visited-file-to-name-at-point~ left as-is (not Org-only).
- init.el: matching autoloads; lisp/doc/latex-export-hook-explained.*
  updated.

* 2026-07-24 Fri org-tools autoload hygiene

- lisp/org-tools.el: keep ;;;###autoload only on interactive entry
  points; drop cookies from export hook callbacks.
- init.el: declare all org-tools interactive autoloads under Local
  libraries (export + buffer); Hyper map keeps bindings only.

* 2026-07-24 Fri Merge Org helpers into org-tools

- lisp/org-tools.el: absorb buffer helpers from scs-org-tools (header,
  creation-date, zwsp, rename); Commentary covers export + buffers.
- lisp/scs-org-tools.el: deleted (no shim); callers retargeted.
- test/org-tools-test.el: renamed from scs-org-tools-test; requires
  org-tools.
- init.el: Hyper autoloads and Org ~require~ use org-tools only.
- readme.org / status.org / insights.org: one library home documented.

* 2026-07-24 Fri In-file Change Log sections

- early-init.el, init.el, lisp/*.el, lisp/scs-convert/, test/*.el,
  bin/*: add file-local ~Change Log~ (newest first, from git plus prior
  ~add:~ / ~fix:~ notes) after Commentary so readers need not dig VCS.
- init.el: restore clarifying ~C-%~ / ~goto-match-paren~ cursor-position note.

* 2026-07-24 Fri Teachable comments across first-party sources

- early-init.el, init.el, lisp/*.el, lisp/scs-convert/scs-convert.el,
  test/*.el, bin/*: expand Commentary, docstrings, and why-comments so
  SCS team readers can follow the config without tribal knowledge.
- Soft welcoming tone; behavior unchanged. Vendored trees (el-get, elpa,
  var, attic) left alone. etc/abbrev.el skipped (generated abbrev data).
- ERT: scs-org-tools and scs-file-header suites still green.

* 2026-07-24 Fri Teachable docs for LAST-UPDATED save hook

- lisp/scs-file-header.el: expand Commentary (problem/solution/policy),
  docstrings, and why-comments for SCS team readers; TODO note about
  possible future ~header2~ evaluation.
- docs/2026-07-23-update-last-updated-on-save-design.org: TODO section
  to consider MELPA ~header2~ later, although we do not like the
  author's posture.

* 2026-07-23 Thu Update LAST-UPDATED on save

- lisp/scs-file-header.el: ~scs/update-last-updated-on-save~ refreshes
  an existing Org ~#+LAST-UPDATED:~ or Lisp ~;; Last-Updated:~ field in
  the file preamble on ~before-save-hook~; never inserts; never bumps
  VERSION or UPDATE.
- init.el: autoload and hook next to trailing-whitespace save hook.
- test/scs-file-header-test.el: ERT for Org/Elisp/Lisp and no-op cases.
- docs/2026-07-23-update-last-updated-on-save-design.org,
  docs/2026-07-23-update-last-updated-on-save-plan.org.

* 2026-07-23 Thu Rename visited file to name at point

- lisp/scs-org-tools.el: ~scs/rename-visited-file-to-name-at-point~
  renames the visited file to the basename at point (Org link, filename,
  or quoted string); confirms ~OLD: rename to: NEW~, then confirm-and-
  replace if the target exists.
- init.el: autoload; ~H-c R~.
- test/scs-org-tools-test.el: ERT for basename extraction and temp-dir
  rename/overwrite.
- docs/2026-07-23-rename-visited-to-name-at-point-design.org,
  docs/2026-07-23-rename-visited-to-name-at-point-plan.org.

* 2026-07-22 Wed Org buffer header ensure

- lisp/scs_org-buffer-template.org: canonical #+KEYWORD order for SCS
  Org file headers.
- lisp/scs-org-tools.el: ~scs/org-ensure-buffer-header~ adds missing
  keywords, keeps existing values, reorders to match the template;
  empty TITLE becomes sentence-case from the filename.
- init.el: autoload ~scs/org-ensure-buffer-header~; ~H-c h~.
- test/scs-org-tools-test.el: ERT coverage for ensure-buffer-header.
- docs/2026-07-22-org-buffer-header-design.org,
  docs/2026-07-22-org-buffer-header-plan.org.

* 2026-07-22 Wed scs/convert (Pandoc markdown -> Org)

- lisp/scs-convert/: ~scs/convert~ replaces region or buffer via Pandoc.
- lisp/scs-convert/tidy-org.lua: strip header IDs (no ~CUSTOM_ID~ drawers).
- init.el: load-path, autoload, ~H-c v~ for ~scs/convert~.
- docs/2026-07-22-scs-convert-design.org: design notes.

* 2026-07-22 Wed Helm find keys and fill-column 72

- init.el: ~C-x C-f~ -> ~helm-multi-files~; ~C-x f~ -> ~helm-find-files~.
- init.el: ~set-fill-column~ moved to ~C-c q~ (near ~M-q~); default
  ~fill-column~ 70 -> 72.

* 2026-07-20 Mon Emacs mail lab scaffold

- docs/2026-07-20-emacs-mail-lab.org: shared Maildir vault (mu4e / notmuch).
- bin/authinfo-pass: PassCmd helper to read ~/.authinfo for mbsync/msmtp.
- docs/msmtprc.mail-lab + ~/.msmtprc: primary send (msmtp SMTPS :465).
- bin/mail-lab-sendmail: Python ssl :465 fallback if msmtp GnuTLS fails.
- Pilot sync lives outside the repo: ~/mail/scs@scs.re/, ~/.mbsyncrc, ~/.authinfo.
- lisp/scs-mail-lab.el: mu4e + notmuch (Gnus dropped); msmtp -a From;
  BBDB contacts via mu cfind JSON; org-msg compose; C-c m m/n/s/c/b/e.
- el-get: bbdb (makefile-temp build), org-msg (depends htmlize).
- ~/.mbsyncrc: three channels (scs@scs.re, priyadarshan@goldenboat.in,
  mail@vsm.in) under ~/mail/<email>/.

* 2026-07-10 Rename changes.org → CHANGELOG.org

- Renamed =changes.org= to =CHANGELOG.org= and updated references in
  =readme.org=, =tasks.org=, =insights.org=, and =cursor-review.org=.

* 2026-07-10 cursor-review fixes

- Corrected smoke tests; expanded library inventory and day-2 ops docs.
- Documented disposable ~custom-file~ (never loaded) and trusted-Org boundary.
- Removed ineffective TRAMP ~standard-value~ mutation.
- Org ID: startup loads persisted locations; ~scs/org-id-rebuild~ and
  ~scs/org-id-report-duplicates~; incremental updates on note save/rename.
- Repaired duplicate Org IDs under =~/notes/zsmistare/=.
- Portable Homebrew discovery and dynamic ~gcc-*~ in =early-init.el=.
- Helm fd: sync for notes, async attach for other roots; cache invalidation.
- Trailing-whitespace predicate; Babel temp-dir cleanup restored.
- Renamed misleading ~my/~ frame/TRAMP helpers to ~scs/~; ~bin/emacs-diagnostic~.
- ~scs/el-get-report~; howm enabled only under ~howm-directory~.
- Dated ~add:~ / ~fix:~ comments on new and changed Lisp from this pass.

* 2026-07-08 Rename GitHub repository to config-emacs

- Renamed ~scsets/emacs~ → ~scsets/config-emacs~ on GitHub.
- Updated ~readme.org~ and parent =~/.config/readme.org= references.

* 2026-06-29 Local =lisp/= libraries, Org creation-date, Hyper prefix

** Local Elisp (=lisp/=)
- Added =lisp/= on ~load-path~ for reusable configuration libraries.
- Moved ~flycheck-org-lint-workaround.el~ from the config root into
  =lisp/=; still loaded from the ~flycheck~ ~use-package~ ~:config~ block
  via ~(require 'flycheck-org-lint-workaround)~.
- Added ~scs-org-tools.el~ with ~scs/org-insert-creation-date~: sets
  lowercase ~:creation-date:~ (ISO ~YYYY-MM-DD~) on the current Org
  headline and normalizes the whole property drawer to lowercase.
- Added ~*.elc~ to =~/.gitignore~ so byte-compiled Elisp is never
  committed.

** Keybindings (macOS Hyper)
- Changed ~H-c~ from a direct ~org-capture~ binding to prefix keymap
  ~scs/hyper-c-prefix-map~.
- ~H-c c~ → ~org-capture~; ~H-c d~ and ~H-c H-d~ →
  ~scs/org-insert-creation-date~ (autoloaded at init; command requires
  Org mode).
- macOS Fn+C opens Control Center and steals ~H-c~. Karabiner rule in
  =~/.config/karabiner/karabiner.json~ (Emacs only): Fn+C →
  Cmd+Ctrl+Opt+C. ~init.el~ ~key-translation-map~: ~C-M-s-c~ → ~H-c~
  (lowercase ~s~ is Super, not Shift).

** yasnippet
- Added ~yasnippet~ via el-get; ~yas-global-mode~; ~company-yasnippet~
  backend (Helm unchanged — snippets are in-buffer, not minibuffer).
- Personal collection in =snippets/= with ~text-mode~ (shared), ~org-mode~,
  ~emacs-lisp-mode~, and ~lisp-mode~ (Common Lisp / SLIME). Each Lisp
  and Org table inherits ~text-mode~ via ~.yas-parents~.
- Org ~meta~ snippet inserts file ~#+title:~ / ~#+author:~ / ~#+date:~
  (ISO date via backquote Elisp). See =insights.org= for the ~${n:$(...)}~
  vs ~${n:\`(form)\`}~ pitfall on dynamic defaults.

** Documentation
- Updated =readme.org=, =status.org=, and =insights.org= for =lisp/=,
  Flycheck workaround loading, creation-date command, Hyper prefix, and
  ~.elc~ gitignore policy.

* 2026-06-21 el-get refresh, package cleanup, Helm workaround

** el-get and packages
- Updated el-get checkout to 5.2 (~447b7ef~) via git pull and
  ~el-get-self-update~.
- Ran ~el-get-update-all~ for all installed packages.
- Fixed ~.status.el~: ~el-get~ was ~required~ without a recipe, which
  broke ~el-get-self-update~ (~el-get-package-method nil~ / recipe errors).
- Removed orphaned el-get installs not declared in ~init.el~: ~corfu~,
  ~marginalia~, ~orderless~ (~marginalia~ / ~orderless~ blocked
  ~el-get-update-all~ because they had no el-get recipe).

** Init fixes after update
- ~company~: ~require 'company-capf~ before touching
  ~company-capf-disabled-functions~ (void-variable on startup).
- ~hl-todo~: added ~cond-let~ to the el-get recipe ~:depends~ (hl-todo
  requires it but did not declare it).
- ~helm~: ~BUG(helm)~ workaround for ~helm-ff--in-backup-directory~
  calling ~file-equal-p~ on nil cdrs in ~backup-directory-alist~
  (triggered by ~no-littering-theme-backups~ during ~helm-find-files~).
  Fixed upstream in helm ~0aa89912~ (#2756); local override removed.

** org-mode / org-contrib
- Both already managed by el-get: ~org~ as ~:type builtin~, ~org-contrib~
  as git from sr.ht. No recipe changes needed.

* 2026-05-11 Activate Helm as the minibuffer system

- Added configuration-owned el-get recipes for ~async~, ~wfnames~, and
  ~helm~, so Helm does not depend on generated ELPA recipes.
- Enabled Helm with explicit module loads for the commands bound by this
  configuration: ~helm-M-x~, ~helm-find-files~, ~helm-mini~,
  ~helm-buffers-list~, ~helm-filtered-bookmarks~, ~helm-show-kill-ring~,
  ~helm-occur~, and ~helm-imenu~.
- Bound Helm over the old minibuffer entry points: ~M-x~, ~C-x C-f~,
  ~C-x b~, ~C-x C-b~, ~C-x r b~, ~M-y~, ~M-s o~, and the macOS Hyper
  shortcuts for command, buffer, and imenu lookup.
- Disabled built-in Fido and removed the active ~marginalia~ and
  ~orderless~ declarations so Helm is the only active minibuffer
  completion UI. Corfu and Cape remain active for in-buffer completion.
- Made the org-id cache setup non-interactive and made
  ~org-id-extra-files~ handling tolerate Org's default symbolic value,
  which keeps batch startup and byte compilation from prompting.

* 2026-05-11 Restore el-get-backed package fetching

- Added el-get bootstrap before package declarations. It loads an existing
  checkout from =el-get/el-get= or clones el-get from GitHub when el-get is
  missing.
- Added a local ~use-package~ ~:el-get~ keyword handler. It supports
  ~:el-get t~, alternate package names, and inline el-get recipe plists,
  without using ~el-get-bundle~.
- Replaced all package-fetching ~:ensure t~ declarations with ~:el-get t~.
- Added configuration-owned el-get recipes and overrides for packages missing
  useful upstream recipes, packages whose upstream recipes were ELPA-only, and
  packages whose primary Git host was not responsive during first-run testing.
- Added explicit recipe dependencies for source installs, including ~dash~ for
  ~eshell-toggle~.
- Moved ~framemove~ off direct ~package-vc-install~ and made
  ~eshell-bookmark~ an explicitly fetched package.
- Removed the unused package archive stale-refresh helpers; el-get now owns
  first-run package installation.

* 2026-04-29 Package archive safeguards

- Added age-gated package archive refresh helpers for first-run installs.
- Kept package archive dependency resolution unpinned so archive priority
  chooses current packages after refreshing metadata.
- Kept the technical notes consolidated in =insights.org=.

* 2026-03-23 Comprehensive review and improvements

** Portability fixes
- Guard ~grep-command "ugrep"~ with ~(executable-find "ugrep")~ -- won't
  break on systems without ugrep.
- All Hyper key bindings wrapped in ~(when (eq system-type 'darwin) ...)~ --
  dead code eliminated on FreeBSD/SmartOS/terminal.

** Hyper key bindings (prefix on H-c plus single-key shortcuts)
- Consolidated into macOS-guarded block with column-header comments.
- ~H-c~ is a prefix keymap (~scs/hyper-c-prefix-map~): ~H-c c~
  (org-capture), ~H-c d~ / ~H-c H-d~ (creation-date; autoload at init).
- Karabiner + ~key-translation-map~ route Fn+C to ~H-c~ in Emacs without
  triggering macOS Control Center (see =insights.org=).
- Other bindings: H-x (execute), H-b (buffer), H-k (kill), H-s (save),
  H-r (revert), H-g (grep), H-n/H-p (next/prev error), H-z (eshell),
  H-o (other-window), H-w (delete-window), H-0..3 (window layout),
  H-t (tramp-cleanup), H-a (org-agenda),
  H-l (org-store-link), H-i (imenu), H-j (avy-goto-char-timer).

** Eshell
- Prompt now shows ~hostname:path $~ -- shows local hostname or remote
  host when on a TRAMP path, so you always know where commands execute.
- Added ~eshell-prompt-regexp~ to match the new prompt format.
- Added aliases: ~ll~, ~la~, ~ff~ (find-file), ~d~ (dired), ~cls~ (clear).

** Completion: company -> corfu
- Disabled company (~:disabled t~, kept for reference).
- Enabled corfu with ~corfu-auto t~, 0.2s delay, prefix length 3, cycling.
- corfu uses native ~completion-at-point~ infrastructure, pairs well with
  orderless for fuzzy matching.

** TRAMP
- Added ~tramp-histfile-override t~ to prevent polluting remote shell history.
- Added keybindings: ~C-c t c~ (tramp-cleanup), ~C-c t r~ (tramp-reopen).
- Enabled FreeBSD gls connection-local profile for ~dasfrp~ host.

** New packages
- *avy* -- Jump to any visible character on screen (H-j, C-c j).
- *diff-hl* -- Show VCS diff markers in margin/fringe. Uses
  ~diff-hl-margin-mode~ in terminal for portability.
- *eat* -- Terminal emulator for eshell. Lighter than vterm (no C
  compilation needed -- works on FreeBSD/SmartOS). Hooks into eshell-mode.
- *fd-dired* -- Use fd instead of find for dired (C-c f). Guarded with
  ~executable-find~.
- *marginalia* -- Annotate minibuffer completions with docstrings, file
  sizes, etc. Works with fido-vertical-mode.
- *orderless* -- Fuzzy/flex completion matching. "ob lan" matches
  "org-babel-load-languages". Sets ~completion-styles~ to ~(orderless basic)~.
- *wgrep* -- Edit grep results in-place and apply changes back to files.

* 2026-03-23 Comment formatting per CLAUDE.md standards

** early-init.el
- Added proper elisp file header with ~;;; Commentary:~ / ~;;; Code:~ sections.
- Converted all ~;;;~ section headings to 60-char heavy dividers.
- Added documentation comments above ~scs--file-name-handler-alist~ and
  ~scs--vc-handled-backends~.
- Added ~;;; early-init.el ends here~ footer.

** init.el
- Expanded file header with ~;;; Commentary:~ section.
- Converted all ~;;;~ major sections to 60-char heavy dividers (8 sections).
- Converted all ~;;;;~ subsections to 60-char light dividers (~40 subsections).
- Added docstrings to ~my-exit~, ~kill-dired-buffers~,
  ~my/switch-to-scratch-buffer~, ~eshell-initialize~.
- Removed ~keyboard-quit~ redefinition -- ~prot/keyboard-quit-dwim~ already
  covers DWIM behavior via remap.
- Removed redundant ~set-input-method~ call -- the ~after-init-hook~
  activation is sufficient.
- Fixed orphaned input-method config that was under the Dired section heading.
- Fixed ~imenu-list~ ~:bind~ quoting -- removed spurious quote before symbol.
- Fixed Unicode violation (U+FFFD literal replaced with ~\uFFFD~ escape).
  Note: ~\xef\xbf\xbd~ would be wrong -- ~\x~ escapes produce characters
  by code point, not UTF-8 byte sequences. ~\uFFFD~ produces the single
  U+FFFD character.
- Normalized Tramp internal dividers to standard 60-char light dividers.
- Fixed em-dash usage to ASCII double-dash in comments.
- Fixed typo: "withing" -> "within" in org-archive comment.
- Normalized blank line semantics throughout.

* 2026-03-23 Fix init.el loading failure

- Fixed case-sensitivity error at line 218: ~Set-input-method~ (capital S)
  changed to ~set-input-method~. Emacs Lisp is case-sensitive; the wrong
  case caused ~void-function~ error that halted loading of the rest of
  init.el.

* 2026-03-07 Howm: semantic filenames and directory change

- Changed ~howm-directory~ from =~/note= to =~/notes=.
- Added ~scs--howm-maybe-rename~: on save, computes desired filename
  from org heading title, ~#+filetags:~, and ISO date. Prompts with
  ~y-or-n-p~ before renaming; user can decline to keep the old name.
- Format: =my-note-title_tag1-tag2_2026-03-07.org=
- Won't rename if target file already exists.
- Helper ~scs--howm-slugify~ converts strings to lowercase hyphenated slugs.

* 2026-03-07 Add howm/org-id integration, fix Emacs server

- Removed ~server-use-tcp t~ for default server; now uses Unix domain sockets
  (works on macOS, FreeBSD, SmartOS). Added second TCP server named "scrim"
  for the Scrim macOS app via ~let~-bound ~server-name~ / ~server-use-tcp~.
- Enhanced howm configuration:
  - Org-mode template with ~#+filetags:~ line.
  - Increased menu/schedule visibility: 30 recent notes, 30-day lookahead.
  - Moved ~howm-keyword-file~ and ~howm-history-file~ into ~no-littering~ paths.
  - Added hooks: ~howm-mode-set-buffer-name~, ~(org-mode . howm-mode)~.
  - Enabled ~howm-view-split-horizontally~ and ~howm-view-contents-limit~.
- Added howm hooks for Org ID linking:
  - New howm notes get an ~:ID:~ property.
  - ~org-id-extra-files~ is populated from the howm directory.

* 2026-03-07 Optimise startup (4s -> 0.6s)

- Removed ~use-package~ bootstrap (~package-install~) — built-in since Emacs 29.
- Fixed ~package-vc~ re-install loop: ~framemove~ and ~psvn~ with ~:vc :rev :newest~
  triggered ~package-refresh-contents~ and "Overwrite?" prompt on every startup.
  Now guarded with ~(unless (package-installed-p ...))~.
- Deferred heavy packages with idle timers:
  ~company~ and ~cape~ at 2s, ~flycheck~ at 3s, ~which-key~ and ~recentf~ at 1s.
- Changed ~org-babel~ from ~:demand t~ to ~:after org~ (loads only when org loads).
- Changed ~org-protocol~ from eager ~require~ to ~with-eval-after-load 'server~.
- Removed ~psvn~ — GitHub repo (emacsmirror/psvn) no longer exists.
  Using built-in ~vc-svn~ instead.
- Removed ~ob-ly~ — no longer included in org-contrib 0.8.
  Removed ~(ly . t)~ from ~org-babel-load-languages~.

* 2026-03-07 Clean and optimise init.el

** Dead code removed
- Test/sample keymaps (~test-command~, ~test-prefix-*-map~) that hijacked ~C-z~.
- Unused ~corfu~ use-package block (Company is the active backend).
- Commented-out code: el-get remnants, helm/outshine/dsvn, dired settings,
  ~remember~ block, theme/dark-mode block.
- Obsolete ~(defalias 'toggle-read-only 'read-only-mode)~ (built-in since 24.3).
- Commented ~helm-mode~ delight reference.

** Bugs fixed
- ~S-f9~ timestamp binding: restored ~org-mode~ guard, fixed ~'(lambda ...)~
  to unquoted ~(lambda ...)~ for byte-compilation.
- Babel language alist: ~(lisp .t)~ -> ~(lisp . t)~ (missing space in cons).
- ~savehist-mode~ activated twice (~:custom~ and ~:config~); removed duplicate.
- Duplicate ~org-confirm-babel-evaluate~ (set both inside and outside ~ob~ block).
- ~vc-handled-backends~ in ~:custom~ conflicted with early-init.el nil-ing;
  moved to ~:config~ with ~setq~.

** Consolidation
- Tramp settings (lines 925-930) moved from loose ~setq~ into ~use-package tramp~.
- Redundant ~ibuffer~ binding removed (kept only ~[remap list-buffers]~).
- Cleaned empty ~:init :config~ from ~(use-package org)~.

* 2026-03-07 Eliminate custom.el

- All ~custom-set-variables~ settings migrated to init.el as proper elisp:
  mode toggles, ~load-theme~, ~set-face-attribute~, ~setq~ calls.
- Removed orphaned denote/xeft settings (no use-package declarations).
- ~custom-file~ redirected to ~/tmp/custom.el~ to prevent regeneration.
- Deleted =custom.el= from config directory.

* 2026-03-07 Add eshell section

- Added ~use-package eshell~ with 50k history, custom prompt, isearch map,
  ~&!~ spawn trick, su/sudo passthrough.
- Added ~eshell-toggle~ (~C-x C-z~), ~eshell-bookmark~, ~eshell-up~, ~eshell-z~.
- Cross-platform for macOS, FreeBSD, SmartOS.

* 2026-03-05 Remove el-get, migrate packages to use-package

- Removed el-get bootstrap and setup block from init.el.
- Migrated ~impatient-mode~ and ~hl-todo~ to ~use-package~ with ~:ensure t~.
- Migrated ~psvn~ to ~use-package~ with ~:vc~ (emacsmirror repo).
- Converted commented-out ~el-get-bundle~ forms (helm, outshine, dsvn)
  to commented-out ~use-package~ forms.
- Removed ~(el-get 'sync)~ call.
- Eliminates startup warnings for framemove/org-contrib
  ("already loaded by package.el").

* 2026-03-05 Clean up early-init.el and init.el

** Comment style standardization
- All section headers use ~;;;~ (top-level) and ~;;;;~ (subsection),
  following standard Emacs Lisp conventions and ~outline-minor-mode~.
- Regular comments use ~;;~, end-of-line annotations use ~;~.
- Replaced decorative separator lines (~======~, ~********~) with
  clean ~;;; ---~ where a visual break is warranted.

** early-init.el
- Renamed ~prot-emacs--~ variable prefix to ~scs--~ (personal config).
- Normalized spacing: single blank between items, double between sections.

** init.el -- section reordering
- Reordered into logical sections: Compatibility, Custom functions,
  Package managers, General settings, Custom file, Keybindings,
  Packages (alphabetical), Sample keymaps, Finalization.
- All commented-out code and annotation tags (~:gem:~, ~:vip:~, etc.)
  preserved in place.

** Formatting fixes
- Consistent 2-space indentation inside ~use-package~ blocks.
- Fixed leading space on ~(use-package company~ form.
- No trailing ~)~ on its own line in ~use-package~ blocks where possible.
- Single blank line between items, double between sections.
- Cleaned up timestamp lambda formatting.

** Specific fixes
- Rebind timestamp from ~<f9>~ to ~<S-f9>~ to avoid conflict with howm's
  ~<f9>~ binding.
- Replaced bare ~custom-set-variables~ call for psvn with ~setq~ to avoid
  conflict with the ~custom-set-variables~ block in =custom.el=.

** What did NOT change
- No code removed; all commented-out blocks preserved.
- No semantic/behavioral changes; same guards, same settings.
- =custom.el= untouched.
- el-get bootstrap logic kept as-is.

* 2026-03-05 Cross-platform adaptation (macOS / FreeBSD / SmartOS)

** Guiding principle: CLI-first, no unnecessary dependencies
- GUI and macOS features are now opt-in via guards.
- On FreeBSD/SmartOS terminal, everything works with zero extra packages.

** early-init.el guards
- ~(tool-bar-mode -1)~ wrapped with ~(display-graphic-p)~.
- ~undecorated-round~ frame parameter wrapped with ~(eq window-system 'ns)~ (macOS GUI only).
- ~set-frame-name~ wrapped with ~(display-graphic-p)~.

** init.el — macOS-specific guards
- Mac modifier keys (~mac-command-modifier~, ~mac-option-modifier~,
  ~mac-control-modifier~, ~ns-function-modifier~) wrapped in ~(eq system-type 'darwin)~.
- ~exec-path-from-shell~ guarded with ~:if (eq system-type 'darwin)~.
- ~reveal-in-osx-finder~ guarded with ~:if (eq system-type 'darwin)~.
- ~server-use-tcp~ (for scrim macOS app) guarded with ~(eq system-type 'darwin)~.

** init.el — executable-find guards
- aspell settings wrapped in ~(executable-find "aspell")~.
- ugrep xref-search-program wrapped in ~(executable-find "ugrep")~.
- howm ripgrep settings wrapped in ~(executable-find "rg")~ with grep fallback.

** init.el — GUI-only guards
- ~pixel-scroll-precision-mode~ additionally guarded with ~(display-graphic-p)~.

** init.el — safe jar paths for org-babel
- ~org-plantuml-jar-path~ and ~org-ditaa-jar-path~ now guarded with
  ~(getenv "PROFILE_DIR")~ and ~file-exists-p~ checks.

** Housekeeping
- Moved =scs.org= to =attic/= (literate config not actively tangled).

* 2026-03-05 Fix Emacs 30 startup failure and clean up stale paths

** Blocking: el-get plist-put crash
- File: =el-get/el-get/el-get-bundle.el:92-94=
- Emacs 30 enforces strict type checking on ~plist-put~; passing a string
  (from ~(car spec)~) instead of a plist caused:
  ~wrong-type-argument plistp "impatient-mode"~
- Fix: initialize ~s~ as ~nil~ instead of ~(car spec)~.
- Deleted stale =el-get-bundle.elc= so the patched source takes effect.

** Unicode smart quote in init.el
- File: =init.el:741=
- A Unicode LEFT SINGLE QUOTATION MARK (U+2018) was used instead of ASCII
  apostrophe (U+0027) in a ~setq~ form, causing ~void-variable~.
- Also removed erroneous quoting of the variable name in ~setq~.

** Undefined function ~user-data~
- File: =init.el:1051=
- Replaced ~(user-data "tramp")~ with ~(no-littering-expand-var-file-name "tramp")~.

** Hardcoded user paths
- Files: =init.el:1014,1024= and =custom.el:11=
- Replaced ~/Users/priyadarshan/...~ with =~/...= and ~expand-file-name~
  for OS-independent home directory resolution.

** Consolidate org-contrib into use-package
- Removed duplicate ~:ensure org-contrib~ from the ~org~ use-package block.
- Replaced ~(el-get-bundle org-contrib)~ with a standalone ~use-package~
  block that requires ~ob-ly~ and ~org-expiry~ in its ~:config~.
- This eliminates the el-get / package.el load-order warning for org-contrib
  and ensures ~ob-ly~ is available for Org Babel LilyPond blocks.

** Add lexical-binding to init.el
- Added ~-*- lexical-binding: t -*-~ to the first line of =init.el=.

** Add RCS Id keywords
- Inserted the RCS Id keyword in =init.el=, =custom.el=, =changes.org=,
  =tasks.org=, =readme.org=, =status.org=, and =insights.org=.
- All files checked into RCS at revision 1.2.

* 2026-03-05 Improve early-init.el

** Critical fix: restore file-name-handler-alist and vc-handled-backends
- =early-init.el= set both to ~nil~ for startup speed but the ~defvar~
  save and ~emacs-startup-hook~ restore were commented out.
- This permanently broke TRAMP, compressed file handling, and VC detection.
- Fix: uncommented save/restore; both are now properly restored after startup.

** Move settings from init.el to early-init.el
- ~native-comp-async-report-warnings-errors~, ~warning-minimum-level~,
  ~byte-compile-warnings~: suppress warnings before any compilation fires.
- ~package-native-compile~, ~package-archives~, ~package-initialize~:
  configure and initialize packages in early-init where they take effect
  earliest, replacing the old ~package-enable-at-startup nil~ pattern.
- ~(tool-bar-mode -1)~: prevent toolbar from drawing then hiding (flicker).

** Add lexical-binding and RCS Id to early-init.el
- Added ~-*- lexical-binding: t -*-~ and RCS Id keyword.

** Add user-emacs-directory for no-littering
- Set ~user-emacs-directory~ explicitly so paths are established before
  anything else runs.

** Updated scs.org
- Removed tangled code that was moved to early-init.el.
- Added notes explaining the relocation.
