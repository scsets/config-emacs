#+title: Emacs Configuration Status
#+author: scs
#+date: 2026-06-29

* Health

| Component          | Status | Notes                                       |
|--------------------+--------+---------------------------------------------|
| Startup            | OK     | Clean startup after el-get refresh          |
| el-get             | OK     | 5.2 (447b7ef); all packages updated         |
| helm               | OK     | Minibuffer UI; #2756 nil backup-dir fix     |
| fido/orderless     | GONE   | Replaced by helm; orderless removed         |
| marginalia         | GONE   | Replaced by helm; marginalia removed        |
| custom.el          | GONE   | All settings in init.el;                    |
|                    |        | custom-file redirected to /tmp/             |
| org                | OK     | el-get builtin recipe                       |
| org-contrib        | OK     | el-get git (sr.ht); org-expiry              |
| scs-org-tools      | OK     | lisp/scs-org-tools.el; H-c H-d creation-date |
| ob-ly              | GONE   | Removed from org-contrib 0.8                |
| eshell             | OK     | eshell-toggle, eshell-z, eshell-up,         |
|                    |        | eshell-bookmark                             |
| psvn               | GONE   | GitHub repo deleted; using vc-svn           |
| company            | OK     | Active in-buffer completion UI              |
| corfu              | GONE   | Removed; company is the active backend        |
| flycheck           | OK     | Deferred 3s; org-lint via lisp/ workaround  |
| framemove          | OK     | Via el-get local recipe                     |
| lisp/ libraries    | OK     | load-path; flycheck + org helpers           |
| SLIME/SLY paths    | CHECK  | Verify ~/lisp/lispworks/ exists             |
| howm               | OK     | Org notes in ~/notes; semantic rename       |
| org-node           | OK     | ID-based linking for howm notes             |
| server (socket)    | OK     | Default server via Unix domain socket       |
| server (TCP/scrim) | OK     | Second server for Scrim macOS app           |
| no-littering       | OK     | Managing var/etc directories                |
| tramp              | OK     | Settings consolidated in use-package        |
| lexical-binding    | OK     | Enabled in init.el and early-init.el        |
| RCS                | OK     | Id keywords in all tracked files            |
| gitignore (.elc)   | OK     | *.elc ignored; do not commit bytecode       |

* Emacs Version

Tested on Emacs 30.2 / 31.x.

* Target Platforms

| Platform | Mode    | Status | Notes                                    |
|----------+---------+--------+------------------------------------------|
| macOS    | GUI(NS) | OK     | Primary development platform             |
| macOS    | CLI     | OK     | Guards skip GUI-only settings            |
| FreeBSD  | CLI     | TARGET | No macOS-specific vars; exec guards      |
| SmartOS  | CLI     | TARGET | No macOS-specific vars; exec guards      |

* Last Verified

[2026-06-29 Sun] - Added =lisp/= local libraries (~scs-org-tools~,
~flycheck-org-lint-workaround~). ~H-c~ Hyper prefix (~H-c c~ capture,
~H-c H-d~ creation-date). Documented in =readme.org=, =changes.org=,
=insights.org=. Git ignores ~*.elc~.

[2026-06-21 Sat] - el-get updated to 5.2; full ~el-get-update-all~ via emacsclient.
Removed orphaned ~corfu~, ~marginalia~, and ~orderless~. Fixed ~.status.el~ el-get
entry, ~company-capf~ require, and ~hl-todo~ ~cond-let~ dependency. Helm
updated; ~file-equal-p~ nil with ~no-littering-theme-backups~ fixed upstream
(#2756, ~0aa89912~).
[2026-05-11 Mon] - Package fetching moved from ~use-package :ensure t~ and ~package-vc~ to el-get through the local ~:el-get~ keyword. First-run bootstrap and package installation completed in a temporary profile, followed by a clean post-install startup. Helm is now installed through el-get and active for minibuffer completion, replacing the Fido/Orderless/Marginalia stack. A warmed-profile smoke test and byte-compile pass also completed; the byte-compile pass only reported existing undefined-function warnings.
