#+title: Emacs Configuration Tasks
#+author: scs

* Document role

Actionable outstanding and completed tasks.  Rationale lives in
=plan.org=; history of user-visible changes lives in =CHANGELOG.org=.

* DONE cursor-review.org findings (2026-07-10)
CLOSED: [2026-07-10 Fri]
- Docs: smoke tests, library inventory, custom-file policy, trusted Org,
  dependency matrix, day-2 ops, document-role lines
- Removed TRAMP ~standard-value~ no-op; renamed ~my/~ TRAMP and frame hooks
- Org ID: load persisted locations; ~scs/org-id-rebuild~ /
  ~scs/org-id-report-duplicates~; incremental update on note save/rename
- Fixed duplicate Org IDs in =~/notes/zsmistare/=
- Dynamic Homebrew GCC (~gcc-*~); Helm async for non-notes roots
- Whitespace predicate; Babel temp cleanup restored; ~bin/emacs-diagnostic~
- ~scs/el-get-report~; howm scoped to notes directory

* TODO From plan.org (2026-07-08)
See =plan.org= for rationale and proposed keybindings.

** High priority
- [ ] Verify ~/lisp/lispworks/lw-console path exists (SLIME)
- [X] Dynamic Homebrew GCC driver in ~early-init.el~ (not hard-coded ~gcc-16~)
- [ ] ~wgrep~ workflow — hook or key after grep / occur
- [ ] ~diff-hl~ navigation keys (~C-c v~ prefix or similar)

** Medium priority
- [ ] CLI parity for macOS Hyper keys (~C-c~ mirrors)
- [ ] Consolidate ~avy~ (~H-j~ vs ~C-c j~)
- [ ] Bind ~howm-rename-to-slug~ when slug rename is trusted
- [ ] Configure ~minions~ or remove if unused
- [ ] Decide on full Magit vs ~vc~ + ~diff-hl~
- [ ] Platform smoke test on FreeBSD and SmartOS CLI

** Lower priority
- [ ] ~keycast~ behind ~C-c h k~ or M-x only
- [ ] ~impatient-mode~ — bind or remove from el-get
- [ ] ~flyspell~ — enable with aspell guard or delete dead config
- [ ] ~clipboard-kill-region~ on ~C-c k~ — enable or drop comment
- [ ] Helm project search (~helm-rgrep~ / similar)

* DONE Fix init load, quit chord, native-comp, and repo rename
CLOSED: [2026-07-08 Wed]
- ~scs/apply-default-font~: ~set-face-attribute~ frame arg (~or frame t~)
- ~C-x C-c~ quit hint + transient map (~scs/quit-prefix~)
- Homebrew ~gcc~ + ~scs/setup-macos-native-comp~ in ~early-init.el~
- GitHub repo renamed to ~scsets/config-emacs~; ~readme.org~ Git/SVN docs
- macOS modifier keys moved to ~early-init.el~

* DONE Remove BUG(helm) workaround when upstream fixes helm-files.el
CLOSED: [2026-06-21 Sat]
Upstream #2756 / ~0aa89912~ adds ~(and f (file-equal-p f ...))~; local
override removed from ~init.el~.

* DONE el-get refresh and package cleanup
CLOSED: [2026-06-21 Sat]
Updated el-get to 5.2, ran ~el-get-update-all~, fixed ~.status.el~,
removed orphaned ~corfu~ / ~marginalia~ / ~orderless~, fixed company and
hl-todo startup errors; helm #2756 tracked separately.

* DONE Activate Helm for minibuffer completion
CLOSED: [2026-05-11 Mon]
Added explicit el-get recipes for ~async~, ~wfnames~, and ~helm~.
Enabled Helm for ~M-x~, file finding, buffer switching, bookmarks, kill
ring, occur, and imenu-oriented workflows, replacing the
Fido/Orderless/Marginalia minibuffer stack.

* DONE Restore el-get-backed package fetching
CLOSED: [2026-05-11 Mon]
Added el-get bootstrap, a local ~use-package :el-get~ keyword handler,
configuration-owned el-get recipes, and converted external package
declarations away from ~:ensure t~ and direct ~package-vc-install~.

* DONE Remove el-get, migrate all packages to use-package
CLOSED: [2026-03-05 Thu]
Removed el-get bootstrap. Migrated impatient-mode, hl-todo, psvn
to use-package.

* DONE Fix Emacs 30 plist-put startup crash
CLOSED: [2026-03-05 Thu]
Patched =el-get-bundle.el= and deleted stale bytecode.

* DONE Fix Unicode smart quote in init.el
CLOSED: [2026-03-05 Thu]
Replaced U+2018 with ASCII apostrophe on line 741.

* DONE Replace undefined ~user-data~ function
CLOSED: [2026-03-05 Thu]
Using ~no-littering-expand-var-file-name~ instead.

* DONE Make user paths OS-independent
CLOSED: [2026-03-05 Thu]
Replaced hardcoded =/Users/priyadarshan/= with =~/= and ~expand-file-name~.

* DONE Consolidate org-contrib into use-package
CLOSED: [2026-03-05 Thu]
Single ~use-package org-contrib~ block. ~ob-ly~ later removed (gone from
org-contrib 0.8).

* DONE Add RCS Id keywords and lexical-binding
CLOSED: [2026-03-05 Thu]
All tracked files now carry the RCS Id keyword.

* DONE Fix file-name-handler-alist / vc-handled-backends restore in early-init.el
CLOSED: [2026-03-05 Thu]
Restore was commented out — TRAMP and VC were permanently broken.

* DONE Improve early-init.el
CLOSED: [2026-03-05 Thu]
Moved compilation, package, and UI settings from init.el.

* DONE Clean, optimise init.el
CLOSED: [2026-03-07 Sat]
- Removed dead code: test keymaps, corfu, commented blocks, toggle-read-only alias
- Fixed bugs: quoted lambda, babel spacing, savehist double-activate
- Fixed vc-handled-backends early-init timing conflict
- Removed duplicate org-confirm-babel-evaluate

* DONE Eliminate custom.el
CLOSED: [2026-03-07 Sat]
All settings moved to init.el. ~custom-file~ redirected to =/tmp/=.
Removed orphaned denote/xeft settings.

* DONE Add eshell section
CLOSED: [2026-03-07 Sat]
Added eshell, eshell-toggle (C-x C-z), eshell-bookmark, eshell-up, eshell-z.
Cross-platform for macOS, FreeBSD, SmartOS.

* DONE Howm semantic filenames (title_tags_date.org)
CLOSED: [2026-03-07 Sat]
Added ~scs--howm-maybe-rename~ hook on save. Prompts before renaming.
Changed howm directory from ~/note to ~/notes.

* DONE Add howm/org-id integration
CLOSED: [2026-03-07 Sat]
Enhanced howm with Org IDs, better template, no-littering paths.
Switched Emacs server from TCP to Unix sockets; added separate TCP server for Scrim.

* DONE Optimise startup time (4s -> 0.6s)
CLOSED: [2026-03-07 Sat]
- Removed use-package bootstrap (built-in since Emacs 29)
- Fixed package-vc re-install loop for framemove (guard with package-installed-p)
- Removed psvn (repo gone from GitHub); use built-in vc-svn
- Removed ob-ly (gone from org-contrib 0.8)
- Deferred heavy packages: company, cape, flycheck, which-key, recentf
- Deferred org-babel to :after org
- Deferred org-protocol to after server starts
- Consolidated tramp settings into use-package block
