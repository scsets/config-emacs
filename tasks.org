# $Id: tasks.org,v 1.5 2026/03/05 17:15:20 scs Exp $
#+title: Emacs Configuration Tasks
#+author: scs

* TODO Resolve el-get / package.el load-order warning for framemove
=framemove= is still loaded by both ~package.el~ and ~el-get~.
Choose one package manager to silence the warning.
(org-contrib warning resolved — now loaded solely via ~use-package~.)

* TODO Verify ~/lisp/lispworks/lw-console path exists
The SLIME and SLY configs reference =~/lisp/lispworks/lw-console=.
Confirm this binary exists for the current user, or update the path.

* DONE Fix Emacs 30 plist-put startup crash
CLOSED: [2026-03-05]
Patched =el-get-bundle.el= and deleted stale bytecode.

* DONE Fix Unicode smart quote in init.el
CLOSED: [2026-03-05]
Replaced U+2018 with ASCII apostrophe on line 741.

* DONE Replace undefined ~user-data~ function
CLOSED: [2026-03-05]
Using ~no-littering-expand-var-file-name~ instead.

* DONE Make user paths OS-independent
CLOSED: [2026-03-05]
Replaced hardcoded =/Users/priyadarshan/= with =~/= and ~expand-file-name~.

* DONE Consolidate org-contrib into use-package; add ob-ly
CLOSED: [2026-03-05]
Removed ~el-get-bundle org-contrib~ and duplicate ~:ensure org-contrib~.
Single ~use-package org-contrib~ block now requires ~ob-ly~ and ~org-expiry~.

* DONE Add RCS Id keywords and lexical-binding
CLOSED: [2026-03-05]
All tracked files now carry the RCS Id keyword. Added ~lexical-binding: t~ to =init.el=.

* DONE Fix critical file-name-handler-alist / vc-handled-backends bug in early-init.el
CLOSED: [2026-03-05]
Restore was commented out — TRAMP and VC were permanently broken.

* DONE Improve early-init.el: move compilation, package, and UI settings
CLOSED: [2026-03-05]
Moved ~native-comp-async-report-warnings-errors~, ~warning-minimum-level~,
~byte-compile-warnings~, ~package-native-compile~, ~package-archives~,
~package-initialize~, and ~tool-bar-mode~ from =init.el= to =early-init.el=.
Added lexical-binding and RCS Id keyword.
