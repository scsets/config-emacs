# $Id: readme.org,v 1.7 2026/03/07 17:16:56 scs Exp $
#+title: Emacs Configuration
#+author: scs

* Overview

Personal Emacs configuration for Emacs 30.2 and Emacs 31.x.

Uses built-in ~use-package~ for package declarations and el-get for
package fetching through the local ~:el-get~ keyword. Local el-get
recipes cover packages that do not have suitable upstream recipes. The
~no-littering~ package keeps data and cache files organized under =var/=
and =etc/=.

Reusable Emacs Lisp helpers live under =lisp/= (see *Local libraries*
below). Byte-compiled =~.elc~= files are gitignored.

* Structure

| File / directory  | Purpose                                    |
|-------------------+--------------------------------------------|
| =init.el=         | Main configuration (all settings)          |
| =early-init.el=   | Early initialization (GC, UI, packages)    |
| =lisp/=           | Reusable local Elisp libraries             |
| =changes.org=     | Changelog of manual fixes and changes      |
| =plan.org=        | Improvement roadmap and keybinding proposals |
| =tasks.org=       | Outstanding and completed tasks              |
| =status.org=      | Current configuration health status        |
| =insights.org=    | Technical notes and lessons learned         |

No =custom.el= — all settings live in =init.el=. The ~custom-file~
variable is redirected to =/tmp/= so Emacs customize writes never
pollute the config.

* Local libraries (=lisp/=)

=init.el= adds =lisp/= to ~load-path~ once at startup. Each library
~NAME.el~ must end with ~(provide 'NAME)~ and is loaded with
~(require 'NAME)~.

| Library                          | Loaded from   | Purpose                          |
|----------------------------------+---------------+----------------------------------|
| =scs-org-tools.el=               | ~org~ hook; Hyper prefix autoload | Org helpers; ~scs/org-insert-creation-date~ sets lowercase ~:creation-date:~ (ISO date) on the current headline |
| =flycheck-org-lint-workaround.el= | ~flycheck~ block | Advice around broken Flycheck/org-lint line-cell handling; auto-enabled by ~scs/flycheck-setup-org-lint-workaround~ |

See =insights.org= for property-drawer behaviour, the ~H-c~ Hyper prefix,
and the Karabiner + ~key-translation-map~ workaround for macOS Fn+C /
Control Center.

* Revision Control

Git repository: ~scsets/config-emacs~ on GitHub (branch ~trunk~). =~.elc~=
files are gitignored and must not be committed.

Emacs ~vc~ is configured for Git and SVN elsewhere in the tree; see
=init.el= and =changes.org=.

* Generated State

Generated runtime state belongs outside Git:

| Path / pattern          | Owner                  | Notes                         |
|-------------------------+------------------------+-------------------------------|
| =var/=                  | no-littering, Org, TRAMP, recentf | Ignored; persistent local state |
| =eln-cache/=            | native compilation     | Ignored; safe to delete after Emacs/GCC upgrades |
| =*.elc=                 | byte compilation       | Ignored; source is authoritative |
| =.org-id-locations=     | legacy Org default     | Ignored; config uses =var/org/id-locations.el= |

~early-init.el~ sets ~load-prefer-newer~ so stale bytecode does not shadow
newer source if an ignored =~.elc~= file is accidentally left behind.

* Smoke Tests

Useful checks after startup-sensitive edits:

#+begin_src sh
emacs --debug-init --batch -l init.el --eval "(require 'org)"
emacs --fg-daemon=scs-config-smoke --debug-init
#+end_src

* Key Packages

- *Completion:* helm for minibuffer commands, company (with yasnippet) and cape for in-buffer completion
- *Snippets:* yasnippet with personal templates in =snippets/= (Org, Emacs Lisp, Common Lisp, shared text-mode). Org triggers include ~meta~ (file ~#+title:~ / author / date), ~src~, ~props~, ~hd~; see =insights.org=.
- *Shell:* eshell, eshell-toggle, eshell-z, eshell-up
- *Org:* org-mode (el-get builtin), org-contrib (el-get git; org-expiry)
- *Notes:* howm (=~/notes=, semantic filenames), org-id (ID linking)
- *Lint:* flycheck (with local org-lint workaround in =lisp/=)
- *Lisp:* slime (LispWorks)
- *UI:* hl-todo, delight, which-key, minions
- *Files:* no-littering, recentf
- *Remote:* tramp
- *VCS:* vc (Git, SVN), framemove (via el-get local recipe)

* macOS Hyper keys

On macOS, Caps Lock is remapped to Fn (Karabiner) and Fn is Emacs
Hyper (~ns-function-modifier~). Most Hyper bindings are single-key
shortcuts (see =init.el=). ~H-c~ is a /prefix/ keymap:

| Chord     | Command                        |
|-----------+--------------------------------|
| ~H-c c~   | ~org-capture~                  |
| ~H-c d~   | ~scs/org-insert-creation-date~ |
| ~H-c H-d~ | ~scs/org-insert-creation-date~ |

~H-c d~ (plain ~d~ after the prefix) is the usual creation-date chord;
~H-c H-d~ also works. Both require Org mode; the command errors
elsewhere.

macOS steals raw Fn+C (Control Center). Karabiner rewrites Fn+C inside
Emacs to a proxy chord; ~init.el~ translates that back to ~H-c~. See
=insights.org= (*Hyper ~H-c~ prefix and macOS Fn+C*) for the rule,
~key-translation-map~ entry, and the ~C-M-s~ vs ~C-M-S~ pitfall.

* Target Platforms

macOS (GUI + CLI), FreeBSD (CLI), SmartOS (CLI).
